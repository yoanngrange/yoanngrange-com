name = "pain-api"
main = "src/index.js"
compatibility_date = "2026-09-01"

[vars]
AIRTABLE_BASE = "appzrC3TnjonofSp2"
ALLOWED_ORIGINS = "https://yoanngrange.com,https://www.yoanngrange.com"

# Secrets (à définir avec `npx wrangler secret put NOM`) :
#   AIRTABLE_TOKEN   — token Airtable, scope data.records:read SEUL (base Pain voisins)
#   FREE_MOBILE_USER   — identifiant "Notifications par SMS" (mobile.free.fr/moncompte → Options)
#   FREE_MOBILE_APIKEY — clé associée
